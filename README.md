# Greeting Card

This project is a simple birthday greeting card webpage built using only HTML and CSS. It displays a clean card layout with a “Happy Birthday” message, a short wish, and two action buttons.